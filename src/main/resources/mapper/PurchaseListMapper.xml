<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.jxc.dao.PurchaseListDao">

    <resultMap id="BaseResultMap" type="com.atguigu.jxc.entity.PurchaseList">
        <result column="purchase_list_id" property="purchaseListId" jdbcType="INTEGER" />
        <result column="purchase_number" property="purchaseNumber" jdbcType="VARCHAR" />
        <result column="purchase_date" property="purchaseDate" jdbcType="VARCHAR" />
        <result column="amount_paid" property="amountPaid" jdbcType="DECIMAL" />
        <result column="amount_payable" property="amountPayable" jdbcType="DECIMAL" />
        <result column="remarks" property="remarks" jdbcType="VARCHAR" />
        <result column="state" property="state" jdbcType="INTEGER" />
        <result column="supplier_id" property="supplierId" jdbcType="INTEGER" />
        <result column="user_id" property="userId" jdbcType="INTEGER" />
    </resultMap>

    <insert id="save" useGeneratedKeys="true" keyProperty="purchaseListId" parameterType="com.atguigu.jxc.entity.PurchaseList">
        INSERT INTO t_purchase_list
        (
        purchase_number, amount_paid, amount_payable, purchase_date,
        remarks, state,supplier_id,user_id
        )
        VALUES
        (
        #{purchaseNumber}, #{amountPaid}, #{amountPayable}, #{purchaseDate}, #{remarks},
        #{state}, #{supplierId},#{userId}
        )
    </insert>

    <select id="list" resultMap="BaseResultMap">
        SELECT * 
        FROM t_purchase_list
        <trim prefix="WHERE" prefixOverrides=" OR ">
            <if test="purchaseNumber != '' and purchaseNumber != null">
                t_purchase_list.purchase_number LIKE CONCAT('%', #{purchaseNumber} ,'%') AND
            </if>
            <if test="supplierId != '' and supplierId != null">
                t_purchase_list.supplier_id LIKE CONCAT('%', #{supplierId} ,'%') AND
            </if>
            <if test="state != '' and state != null">
                t_purchase_list.state = #{state} AND
            </if>
            <if test="sTime != null and sTime != ''">t_purchase_list.purchase_date <![CDATA[>=]]> #{sTime} AND </if>
            <if test="eTime != null and eTime != ''">t_purchase_list.purchase_date <![CDATA[<=]]> #{eTime}</if>
        </trim>
    </select>

</mapper>